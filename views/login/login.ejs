<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Connexion - SangVital</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Animate.css -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <!-- Custom CSS -->
    <style>
        /* Fond similaire à la page d'accueil */
        .background-image {
            background: url('../images/blood-donation-bg.jpg') center center/cover no-repeat;
            position: relative;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
        }

        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.6);
            z-index: 1;
        }

        .login-box {
            position: relative;
            z-index: 2;
            /* au-dessus de l'overlay */
            background-color: rgba(255, 255, 255, 0.9);
            color: #333;
            padding: 2rem;
            width: 320px;
            border-radius: 8px;
            text-align: center;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }

        .login-box .logo {
            width: 60px;
            margin-bottom: 1rem;
        }

        .login-box h2 {
            margin-bottom: 1.5rem;
            color: #1E3A5F;
        }

        .login-box input {
            width: 100%;
            padding: 0.75rem;
            margin-bottom: 1rem;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .login-box button {
            width: 100%;
            padding: 0.75rem;
            background-color: #1E3A5F;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-weight: bold;
        }

        .login-box button:hover {
            background-color: #16324f;
        }

        .login-box a {
            color: #1E3A5F;
            text-decoration: none;
        }
    </style>
</head>

<body>
    <section class="background-image">
        <div class="overlay"></div>
        <div class="login-box animate__animated animate__fadeInDown">
            <img src="../images/logo.png" alt="Logo de SangVital" class="logo">
            <h2>Connexion</h2>
            
            <form action="/login" method="post">
                <input type="text" name="username" placeholder="Nom d'utilisateur" required>
                <input type="password" name="password" placeholder="Mot de passe" required>
                <button type="submit" class="animate__animated animate__pulse">Se connecter</button>
            </form>
            <p><a href="#">Mot de passe oublié ?</a></p>
            <p>Pas encore inscrit ? <a href="/registration">Créer un compte</a></p>
        </div>
    </section>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>