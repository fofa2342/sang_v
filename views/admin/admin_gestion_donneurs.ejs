<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Gestion Dons - Administrateur</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <style>
    html, body {
      height: 100%;
      margin: 0;
      padding: 0;
    }
    .wrapper {
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      margin-top: 80px;
    }
    .content {
      flex: 1;
      padding: 2rem;
      background-color: #f5f5f5;
    }
    footer {
      background-color: #1E3A5F;
      color: #fff;
      text-align: center;
      padding: 1rem;
    }
    .navbar-brand {
      font-weight: bold;
      font-size: 1.5rem;
    }
    .nav-link.active {
      background-color: #1E3A5F;
      border-radius: 4px;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow">
    <div class="container">
      <a class="navbar-brand" href="#">SangVital</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNavbar"
              aria-controls="adminNavbar" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="adminNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/admin/">Accueil</a></li>
          <li class="nav-item"><a class="nav-link" href="/admin/ges_utilisateurs">Gestion Utilisateurs</a></li>
          <li class="nav-item"><a class="nav-link active" href="/admin/ges_dons">Gestion Dons</a></li>
          <li class="nav-item"><a class="nav-link" href="/admin/statistiques">Statistiques</a></li>
          <li class="nav-item">
            <a class="nav-link btn btn-outline-light ms-2" href="/">Déconnexion</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="wrapper">
    <div class="content animate__animated animate__fadeInUp">
      <h2>Gestion des Dons</h2>
      <p>Ajoutez, modifiez ou supprimez des dons de sang.</p>

      <!-- Bouton pour ouvrir la modale "Ajouter un Don" -->
      <button type="button" class="btn btn-success mb-3" data-bs-toggle="modal" data-bs-target="#addDonModal">
        Ajouter un Don
      </button>

      <!-- Tableau des dons -->
      <table class="table table-striped mt-4">
        <thead class="table-dark">
          <tr>
            <th>ID</th>
            <th>ID Donneur</th>
            <th>Donneur</th>
            <th>Groupe Sanguin</th>
            <th>Date</th>
            <th>Type</th> 
            <th>Volume (ml)</th>
            <th>État</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% dons.forEach(don => { %>
            <tr>
              <td><%= don.id_don %></td>
              <td><%= don.id_user %></td>
              <td><%= don.donneur_nom %> <%= don.donneur_prenom %></td>
              <!-- Nouvelle colonne pour afficher le groupe sanguin -->
              <td>
                <!-- On concatène type_groupe + rhesus, ex: "A+" ou "O-" -->
                <%= don.type_groupe %><%= don.rhesus %>
              </td>
              <td><%= don.date_don %></td>
              <td><%= don.type_don %></td>
              <td><%= don.volume_ml %></td>
              <td><%= don.etat %></td>
              <td>
                <!-- On peut retirer la div.btn-group si on veut un style plus espacé -->
                <div class="d-flex gap-2">
                  <!-- Bouton Modifier -->
                  <button type="button" class="btn btn-outline-primary btn-sm"
                          data-bs-toggle="modal"
                          data-bs-target="#editDonModal_<%= don.id_don %>">
                    <i class="bi bi-pencil-square"></i> Modifier
                  </button>
      
                  <!-- Formulaire Supprimer -->
                  <form action="/admin/delete_don/<%= don.id_don %>" method="POST">
                    <button class="btn btn-outline-danger btn-sm"
                            onclick="return confirm('Supprimer ce don ?')">
                      <i class="bi bi-trash"></i> Supprimer
                    </button>
                  </form>
                </div>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
      
    </div>
    <footer>
      <p>&copy; 2025 SangVital. Tous droits réservés.</p>
    </footer>
  </div>

  <!-- Modale "Ajouter un Don" -->
  <div class="modal fade" id="addDonModal" tabindex="-1" aria-labelledby="addDonModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="addDonModalLabel">Ajouter un Don</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <form action="/admin/add_don" method="POST">
          <div class="modal-body">
            <div class="mb-3">
              <label>ID Donneur</label>
              <input type="number" name="id_user" class="form-control" required>
            </div>
            <div class="mb-3">
              <label>Date</label>
              <input type="datetime-local" name="date_don" class="form-control" required>
            </div>
            <div class="mb-3">
              <label>Type de Don</label>
              <select name="type_don" class="form-select" required>
                <option value="SANG_TOTAL">Sang Total</option>
                <option value="PLAQUETTES">Plaquettes</option>
              </select>
            </div>
            <div class="mb-3">
              <label>Volume (ml)</label>
              <input type="number" name="volume_ml" class="form-control" required>
            </div>
            <div class="mb-3">
              <label>État</label>
              <select name="etat" class="form-select" required>
                <option value="EN_COURS">En Cours</option>
                <option value="VALIDE">Validé</option>
                <option value="REFUSE">Refusé</option>
              </select>
            </div>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
            <button type="submit" class="btn btn-primary">Enregistrer</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Modales "Modifier un Don" (placées en dehors du tableau) -->
  <% dons.forEach(don => { %>
  <div class="modal fade" id="editDonModal_<%= don.id_don %>" tabindex="-1"
       aria-labelledby="editDonLabel_<%= don.id_don %>" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="editDonLabel_<%= don.id_don %>">Modifier le Don #<%= don.id_don %></h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <form action="/admin/edit_don/<%= don.id_don %>" method="POST">
          <div class="modal-body">
            <div class="mb-3">
              <label>ID Donneur</label>
              <input type="number" name="id_user" class="form-control" value="<%= don.id_user %>" required>
            </div>
            <div class="mb-3">
              <label>Date</label>
              <input type="datetime-local" name="date_don" class="form-control" value="<%= don.date_don %>" required>
            </div>
            <div class="mb-3">
              <label>Type de Don</label>
              <select name="type_don" class="form-select" required>
                <option value="SANG_TOTAL" <%= don.type_don === 'SANG_TOTAL' ? 'selected' : '' %>>Sang Total</option>
                <option value="PLAQUETTES" <%= don.type_don === 'PLAQUETTES' ? 'selected' : '' %>>Plaquettes</option>
              </select>
            </div>
            <div class="mb-3">
              <label>Volume (ml)</label>
              <input type="number" name="volume_ml" class="form-control" value="<%= don.volume_ml %>" required>
            </div>
            <div class="mb-3">
              <label>État</label>
              <select name="etat" class="form-select" required>
                <option value="EN_COURS" <%= don.etat === 'EN_COURS' ? 'selected' : '' %>>En Cours</option>
                <option value="VALIDE" <%= don.etat === 'VALIDE' ? 'selected' : '' %>>Validé</option>
                <option value="REFUSE" <%= don.etat === 'REFUSE' ? 'selected' : '' %>>Refusé</option>
              </select>
            </div>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
            <button type="submit" class="btn btn-primary">Enregistrer</button>
          </div>
        </form>
      </div>
    </div>
  </div>
  <% }) %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
