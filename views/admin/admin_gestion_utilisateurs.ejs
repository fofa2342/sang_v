<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Gestion Utilisateurs - Administrateur</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
  <!-- Ajout de Bootstrap Icons pour les icônes -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <style>
    html, body { height: 100%; margin: 0; padding: 0; }
    .wrapper { display: flex; flex-direction: column; min-height: 100vh; margin-top: 80px; }
    .content { flex: 1; padding: 2rem; background-color: #f5f5f5; }
    footer { background-color: #1E3A5F; color: #fff; text-align: center; padding: 1rem; }
    .navbar-brand { font-weight: bold; font-size: 1.5rem; }
    .nav-link.active { background-color: #1E3A5F; border-radius: 4px; }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow">
    <div class="container">
      <a class="navbar-brand" href="#">SangVital</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNavbar"
        aria-controls="adminNavbar" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse" id="adminNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/admin/">Accueil</a></li>
          <li class="nav-item"><a class="nav-link active" href="/admin/ges_utilisateurs">Gestion Utilisateurs</a></li>
          <li class="nav-item"><a class="nav-link" href="/admin/ges_dons">Gestion Dons</a></li>
          <li class="nav-item"><a class="nav-link" href="/admin/statistiques">Statistiques</a></li>
          <li class="nav-item">
            <a class="nav-link btn btn-outline-light ms-2" href="/">Déconnexion</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="wrapper">
    <div class="content animate__animated animate__fadeInUp">
      <h2>Gestion des Utilisateurs</h2>
      <p>Ajoutez, modifiez ou supprimez des comptes utilisateurs.</p>

      <div class="mb-3">
        <a href="/admin/ajouter_utilisateur" class="btn btn-success">
          <i class="bi bi-plus-circle"></i> Ajouter un utilisateur
        </a>
      </div>
      
      <table class="table table-striped mt-4">
        <thead class="table-dark">
          <tr>
            <th>Nom d'utilisateur</th>
            <th>Identifiant</th>
            <th>Rôle</th>
            <th>Email</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% users.forEach(function(user) { %>
          <tr>
            <td><%= user.username %></td>
            <td><%= user.id %></td>
            <td><%= user.role %></td>
            <td><%= user.email %></td>
            <td>
              <!-- Bouton Modifier avec style outline et icône -->
              <a href="/admin/edit_user/<%= user.id %>" class="btn btn-outline-primary btn-sm">
                <i class="bi bi-pencil-square"></i> Modifier
              </a>

              <!-- Bouton Supprimer avec style outline et icône -->
              <form action="/admin/delete_user/<%= user.id %>" method="POST" style="display:inline;">
                <button class="btn btn-outline-danger btn-sm" onclick="return confirm('Supprimer ?')">
                  <i class="bi bi-trash"></i> Supprimer
                </button>
              </form>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <footer>
      <p>&copy; 2025 SangVital. Tous droits réservés.</p>
    </footer>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
