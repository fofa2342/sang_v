<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Dashboard Assistant Social - SangVital</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Animate.css -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <style>
        body {
            background-color: #f5f5f5;
        }
        .navbar-brand {
            font-weight: bold;
            font-size: 1.5rem;
        }
        .content-container {
            margin-top: 80px;
            padding: 2rem;
        }
        footer {
            background-color: #1E3A5F;
            color: #fff;
            text-align: center;
            padding: 1rem;
            margin-top: 2rem;
        }
        .nav-link.active {
            background-color: #1E3A5F;
            border-radius: 4px;
        }
    </style>
</head>

<body class="d-flex flex-column min-vh-100">
    <!-- Barre de navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow">
        <div class="container">
            <a class="navbar-brand" href="/">SangVital</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#assistantNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="assistantNavbar">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/assistant/">Accueil</a></li>
                    <li class="nav-item"><a class="nav-link active" href="/assistant/ges_donneurs/">Gestion Donneurs</a></li>
                    <li class="nav-item"><a class="nav-link" href="/assistant/campagnes/">Campagnes de dons</a></li>
                    <li class="nav-item">
                        <a class="nav-link btn btn-outline-light ms-2" href="/">Déconnexion</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Contenu principal -->
    <main class="container mt-5">
        <div class="content animate__animated animate__fadeInUp">
            <h2>Gestion des Donneurs</h2>
            <p>Ajoutez, modifiez ou supprimez des comptes de donneur.</p>
            <a href="/admin/ajouter_utilisateur" class="btn btn-success mb-3">
                <i class="bi bi-plus-circle"></i> Ajouter un utilisateur
            </a>
            <table class="table table-striped">
                <thead class="table-dark">
                    <tr>
                        <th>ID</th>
                        <th>Nom</th>
                        <th>Groupe Sanguin</th>
                        <th>Date Don</th>
                        <th>Type</th>
                        <th>Volume (ml)</th>
                        <th>État</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% donneurs.forEach(donneur => { %>
                        <tr>
                            <td><%= donneur.userId %></td>
                            <td><%= donneur.userNom %> <%= donneur.userPrenom %></td>
                            <td><%= donneur.type_groupe %> <%= donneur.rhesus %></td>
                            <td><%= donneur.date_don || 'N/A' %></td>
                            <td><%= donneur.type_don || 'N/A' %></td>
                            <td><%= donneur.volume_ml || 'N/A' %></td>
                            <td><%= donneur.etat || 'N/A' %></td>
                            <td>
                                <div class="d-flex gap-2">
                                    <button type="button" class="btn btn-outline-primary btn-sm" data-bs-toggle="modal" data-bs-target="#editDonModal_<%= donneur.userId %>">
                                        <i class="bi bi-pencil-square"></i> Modifier
                                    </button>
                                    <form action="/admin/delete_don/<%= donneur.userId %>" method="POST">
                                        <button class="btn btn-outline-danger btn-sm" onclick="return confirm('Supprimer ce don ?')">
                                            <i class="bi bi-trash"></i> Supprimer
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </main>

    <footer>
        <p>&copy; 2025 SangVital. Tous droits réservés.</p>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
